<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uyr.yusara.dreamhome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestFireBaseActivity" />
        <activity android:name=".RecyclerViewTest.RecycleViewTest" />
        <activity android:name=".Login" />
        <activity android:name=".Modal.Product$Register" />
        <activity android:name=".Register2" />
        <activity android:name=".ProductsActitvity" />
        <activity android:name=".UpdateProductActivity"></activity>
    </application>

</manifest>