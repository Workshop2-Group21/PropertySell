<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uyr.yusara.dreamhome">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />
    ﻿

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestFireBaseActivity" />
        <activity android:name=".RecyclerViewTest.RecycleViewTest" />
        <activity android:name=".Menu.Login" />
        <activity android:name=".Modal.Product$Register" />
        <activity android:name=".Menu.Register2" />
        <activity android:name=".ProductsActitvity" />
        <activity android:name=".UpdateProductActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Profile" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".PostActivity" />
        <activity android:name=".AllPostActivity" />
        <activity android:name=".ClickPostActivity" />
        <activity android:name=".Agent.AllPostActivityAgent" />
        <activity android:name=".FindHouseActivity" />
        <activity android:name=".CommentActivity" />
        <activity android:name=".Agent.ClickPostActivityAgent" />
        <activity android:name=".Menu.ResetPasswordActivity" />
        <activity android:name=".testflip" />
        <activity android:name=".Agent.AllAgentList" />
        <activity android:name=".Agent.AgentProfileDetailActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Customer.MainActivityCustomer"
            android:label="@string/title_activity_main_customer"
            android:theme="@style/AppTheme" />
        <activity android:name=".AllPostActivity2" />
        <activity android:name=".Agent.AllAgentListFromProfile" />
        <activity android:name=".News.NewsMainActivity" />
        <activity android:name=".News.DetailsActivity" />
        <activity android:name=".LoadCalculator"></activity>
    </application>

</manifest>